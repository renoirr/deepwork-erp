export default function ImagePage() {
  return (
    <div className="mx-auto max-w-4xl">
      <div className="card">
        <p className="text-[13px] text-muted-fg">
          1000×1000 규격 이미지를 Google Gemini로 생성합니다. <code className="rounded bg-muted px-1.5 py-0.5 font-mono text-[11.5px] text-fg">GEMINI_API_KEY</code> 발급 후 연결 예정입니다.
        </p>
      </div>
    </div>
  );
}
